const Button = () => {
  return (
    <button
      className="rounded-full bg-repink px-5 py-2 text-white"
      onClick={() => window.open("https://linktr.ee/restore.trd", "_blank")}
    >
      Join us
    </button>
  );
};
export default Button;
